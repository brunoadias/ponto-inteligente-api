# Arquivo com informações ao Heroku para implementação
# Parâmetro $PORT recebe a porta determinada pelo Heroku
# Determinação de qual perfil o Heroku deverá se basear
# Nome do jar que o Heroku deverá colocar no ar 
web: java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar target/ponto-inteligente-0.0.1-SNAPSHOT.jar